main PROC

PUSH AX
PUSH AX
PUSH AX
MOV AX,1
PUSH AX
POP AX 
 MOV BP,SP
MOV [BP+0],AX 
 PUSH AX
POP AX
MOV AX,1
PUSH AX
POP AX 
 MOV BP,SP
MOV [BP+2],AX 
 PUSH AX
POP AX
MOV BP,SP
MOV AX,[BP+0]
PUSH AX
POP AX
CMP AX,0
JE L1

MOV BP,SP
MOV AX,[BP+2]
PUSH AX
POP AX
CMP AX,0
JE L3

MOV AX,3
PUSH AX
POP AX 
 MOV BP,SP
MOV [BP+4],AX 
 PUSH AX
POP AX
JMP L4
L3:

MOV AX,2
PUSH AX
POP AX 
 MOV BP,SP
MOV [BP+4],AX 
 PUSH AX
POP AX
L4:

JMP L2
L1:

MOV BP,SP
MOV AX,[BP+2]
PUSH AX
POP AX
CMP AX,0
JE L5

MOV AX,1
PUSH AX
POP AX 
 MOV BP,SP
MOV [BP+4],AX 
 PUSH AX
POP AX
JMP L6
L5:

MOV AX,0
PUSH AX
POP AX 
 MOV BP,SP
MOV [BP+4],AX 
 PUSH AX
POP AX
L6:

L2:

POP AX
POP AX
POP AX

main ENDP

